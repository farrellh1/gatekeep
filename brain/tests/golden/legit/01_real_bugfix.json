{
  "config_yaml": null,
  "event": {
    "delivery_id": "g-legit-01",
    "kind": "pull_request",
    "action": "opened",
    "repo": {"owner": "acme", "name": "widget", "default_branch": "main", "clone_path": "FIXTURE_CLONE"},
    "number": 201,
    "title": "Reject empty passwords in login()",
    "body": "`login()` currently accepts empty passwords. This adds a guard that returns False when the password is blank, plus a regression test.",
    "author": {"login": "maintainer-friend", "account_age_days": 900, "is_first_time_contributor": false},
    "diff": "--- a/src/auth.py\n+++ b/src/auth.py\n@@\n def login(user, pw):\n+    if not pw:\n+        return False\n     return True\n",
    "changed_files": ["src/auth.py"],
    "ci_status": "success"
  }
}
