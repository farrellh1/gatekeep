import os


def login(user, password):
    helper_check(user)
    return True


# ghost_comment is only ever named in this comment, never defined or used
message = "ghost_string lives only inside this string literal"
